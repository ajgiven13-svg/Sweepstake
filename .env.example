# Optional live fixture/results provider.
FOOTBALL_DATA_TOKEN=

# Optional live outright odds provider. Static odds are already baked into the app.
ODDS_API_TOKEN=

# Required on Vercel if you want one shared draw state for everyone using the link.
# Use Upstash Redis REST credentials, or Vercel KV/Upstash-compatible values.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
